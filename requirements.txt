# python3 -m pip install -r requirements.txt
# 버전은 이 프로젝트가 실제로 돌아간 조합. 올려도 되면 == 를 >= 로 바꿔 쓰면 된다.

# ocr.py: PDF 렌더링 + 헤더/푸터 마스킹
PyMuPDF==1.26.5
pillow==10.4.0

# ocr.py: Unlimited-OCR 실행은 표준 라이브러리(urllib)로 로컬 llama-server(CUDA)의
# /completion에 직접 붙는다. llama-server 자체는 pip 패키지가 아니라 ~/llama.cpp를
# CUDA로 빌드해 둔 바이너리를 쓴다.

# extract.py: 표준 라이브러리만 쓴다(표 서식 기반 파서). 추가 의존성 없음.

# server.py: 업로드 데모 웹 서버. CLI만 쓸 거면 이 셋은 없어도 된다.
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32
